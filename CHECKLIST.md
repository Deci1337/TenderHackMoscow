# Чек-лист Хакатона: TenderHackMoscow (Умный поиск)

Этот чек-лист содержит **все базовые и критические требования** из ТЗ. Отмечайте пункты крестиком `[x]`, когда задача полностью реализована и протестирована.

## 1. Инфраструктура и Данные (Dev 1)
- [x] Поднять базовый бэкенд (FastAPI / Python).
- [x] Развернуть базу данных с поддержкой векторов (PostgreSQL + `pgvector`).
- [x] Написать скрипты-парсеры для исходных датасетов (СТЕ и Контракты).
- [ ] Очистить данные и загрузить их в базу данных (ожидаем файлы).
- [x] Настроить Docker/Docker-compose для деплоя на наш Ubuntu-сервер (без использования внешних облачных API).

## 2. Базовый поиск и NLP (Dev 2)
- [ ] Выбрать и локально развернуть **легковесную** ML-модель для семантики (например, `rubert-tiny2` + FAISS/pgvector).
- [ ] Реализовать морфологический разбор запросов (учет падежей, склонений).
- [ ] Реализовать алгоритм исправления опечаток (быстрый, без LLM, например `SymSpell`).
- [ ] Подключить механизм работы с синонимами.
- [ ] Настроить гибридный поиск (текстовое совпадение + семантическое сходство).

## 3. Персонализация и ML Ранжирование (Dev 2)
- [ ] Сформировать профиль пользователя (ИНН, регион, история прошлых контрактов).
- [ ] Настроить фильтрацию/ранжирование омографов в зависимости от профиля компании (например, больница vs школа).
- [x] Реализовать API для сбора позитивных действий (клики, добавление к сравнению, лайки).
- [x] Реализовать API для сбора **негативных паттернов** (быстрый возврат в поиск, пропуск товаров).
- [ ] Обучить и внедрить модель ранжирования (например, `CatBoost Ranker`), учитывающую историю и действия.

## 4. Динамическая индексация и Объяснимость (Dev 1 & Dev 2)
- [x] Реализовать **динамическую индексацию**: Redis session index — выдача меняется в реальном времени после действий пользователя.
- [x] Сделать модуль объяснимости: rule-based + SQL — отдает причину **почему** конкретный СТЕ выше в выдаче (бейджи на карточке).
- [x] Сделать модуль объяснимости смены направления: `get_session_change_reason()` — UI показывает `RankingChangeNotice` с причиной.

## 5. Фронтенд и UI (Dev 1)
- [x] Инициализировать фронтенд-проект (React + Vite + TypeScript + Tailwind).
- [ ] Сверстать интерфейс **строго по предоставленному UI-киту из Figma** (стилизация под zakupki.mos.ru готова, финальная подгонка после получения кита).
- [x] Интегрировать поисковую строку с бэкендом.
- [x] Вывести на UI подсказки об исправлении опечаток ("Возможно, вы искали: ...").
- [x] Вывести на карточках СТЕ визуальные бейджи/теги объяснимости ("Часто покупают в вашей сфере", "На основе контракта X").
- [x] Настроить отправку телеметрии (клики, время на странице) с фронтенда на бэкенд.

## 6. Подготовка к защите (Product / Dev 1 & 2)
- [x] Составить итоговый список **метрик качества поиска** (NDCG, MRR, MAP и т.д.) с подробным обоснованием выбора. → `METRICS.md`
- [x] Разработать схему реализации в нотации **BPMN** (две дорожки). → `BPMN.md` (Mermaid)
- [x] Описать возможности масштабирования системы (архитектурный план). → `SCALING.md`
- [x] Подготовить сценарии для демо (4 сценария). → `DEMO_SCENARIOS.md`
- [x] Проверить, что решение работает исключительно на наших серверах (Linux) и не использует запрещенные LLM/API — все зависимости offline, внешних запросов нет.